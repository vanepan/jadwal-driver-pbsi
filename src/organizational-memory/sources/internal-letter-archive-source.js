'use strict';

import { makePlaceholderArchiveSource } from './placeholder-archive-source.js';

export const internalLetterArchiveSource = makePlaceholderArchiveSource(
  'internal_letter',
  'Internal Letter documents (inactive placeholder — no real V1 store exists yet, confirmed by research).',
);

export default internalLetterArchiveSource;
