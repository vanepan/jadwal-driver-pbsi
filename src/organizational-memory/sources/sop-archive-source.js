'use strict';

import { makePlaceholderArchiveSource } from './placeholder-archive-source.js';

export const sopArchiveSource = makePlaceholderArchiveSource(
  'sop',
  'Standard Operating Procedure documents (inactive placeholder — no real V1 store exists yet, confirmed by research).',
);

export default sopArchiveSource;
