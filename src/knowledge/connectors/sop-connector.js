'use strict';

import { makePlaceholderConnector } from './placeholder-connector.js';

export const sopConnector = makePlaceholderConnector(
  'sop',
  'Standard Operating Procedure documents (inactive placeholder — not started).',
);

export default sopConnector;
