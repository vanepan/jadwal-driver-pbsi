'use strict';

import { makePlaceholderConnector } from './placeholder-connector.js';

export const memorandumConnector = makePlaceholderConnector(
  'memorandum',
  'Approved Memorandum documents (inactive placeholder — not started).',
);

export default memorandumConnector;
