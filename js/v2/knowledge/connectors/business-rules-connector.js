'use strict';

import { makePlaceholderConnector } from './placeholder-connector.js';

export const businessRulesConnector = makePlaceholderConnector(
  'business_rules',
  "Existing validation/policy engines' rule definitions (inactive placeholder — not started).",
);

export default businessRulesConnector;
