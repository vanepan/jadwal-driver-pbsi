'use strict';

import { makePlaceholderConnector } from './placeholder-connector.js';

export const templatesConnector = makePlaceholderConnector(
  'templates',
  "js/docs/template-registry.js descriptors and report-types.js typedefs (inactive placeholder — not started).",
);

export default templatesConnector;
