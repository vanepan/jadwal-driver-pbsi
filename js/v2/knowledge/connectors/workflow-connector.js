'use strict';

import { makePlaceholderConnector } from './placeholder-connector.js';

export const workflowConnector = makePlaceholderConnector(
  'workflow',
  "Engineering's lifecycle graph and future modules' state machines (inactive placeholder — not started).",
);

export default workflowConnector;
